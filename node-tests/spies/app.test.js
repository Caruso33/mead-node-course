const expect = require('expect');
const jest = require('jest');

const mockCallback = jest.fn(() => true);
// describe('App', () => {
//   it('should call the spy correctly', () => {
//     var spy = jest.fn();//the function you swap for the real one
//     spy();
//     expect(spy).toHaveBeenCalled();
//   });
// });
